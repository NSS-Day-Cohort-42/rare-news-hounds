import React from "react"
import { Button } from "react-bootstrap"

export default () => {
    return (
        <Button type="submit" className="w-50 mx-auto">
            Add Category
        </Button>
    )
}